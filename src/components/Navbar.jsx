import React from "react";
import { Link } from "react-router-dom";

const Navbar = () => {
  return (
    <nav className="navbar">
      <div>(/*...*/)</div>
      <Link to="/">Home</Link>
      <button>Home</button>
    </nav>
    
  );
};

export default Navbar;
